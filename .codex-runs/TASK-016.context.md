# TASK-016 Context

- Scope: component tests for the shared base UI elements in `frontend/src/app/shared/ui/base/`.
- Location: `frontend/src/app/shared/ui/base/*.spec.ts`.
- Approach: strengthen coverage for rendering, accessibility attributes, projected content, and state-specific classes/events.
- Validation target: `npm run build --prefix frontend` and `npm test --prefix frontend`.
