# TASK-117 — Cadastro, Login, Rota Protegida e Logout Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Criar uma infraestrutura Playwright determinística e uma jornada Chromium que cadastre um cliente, confirme o cadastro, autentique com persistência local, sobreviva a refresh, acesse uma rota protegida e encerre a sessão sem conservar acesso.

**Architecture:** O browser continuará executando a SPA real servida pelo Vite, mas toda chamada para `http://localhost:5228/api/v1/**` será controlada por `browserContext.route`. Uma fixture automática criará um backend em memória por teste, limpará cookies e Web Storage antes da primeira navegação, contabilizará cada request e fará o teste falhar por endpoint, método, corpo ou credencial não declarados. A jornada usará somente seletores semânticos e verificará o estado observável da UI, da URL, do storage e da rede.

**Tech Stack:** React 19, TypeScript 5.7, Vite 6, Playwright 1.61, Chromium, Zustand Persist, TanStack Query.

## Global Constraints

- Escopo exclusivo: frontend e documentação da `TASK-117`; não alterar produto, backend ou backlog durante a implementação.
- A task só pode iniciar com `Status: READY` e `TASK-010`, `TASK-111`, `TASK-112`, `TASK-113`, `TASK-114`, `TASK-115` e `TASK-116` em `DONE`.
- Registrar `BASE_COMMIT=$(git rev-parse HEAD)` antes de qualquer alteração de implementação.
- O agente principal atua apenas como orquestrador: explorador, implementador e revisor devem ser agentes distintos.
- Não executar dois agentes com permissão de escrita simultaneamente no mesmo checkout.
- Usar browser route mocks; não iniciar API, PostgreSQL, Docker nem depender de dados externos.
- Cada teste deve começar sem cookies, `localStorage`, `sessionStorage` ou estado do backend simulado.
- Requests não declarados devem abortar imediatamente e produzir uma mensagem com método e URL.
- Contadores devem ser locais ao teste e comparados exatamente no teardown, inclusive após falha no corpo do teste.
- A jornada deve usar `getByRole`, `getByLabel` e assertions de URL; não usar classes CSS, XPath, `data-testid` ou sleeps.
- A persistência escolhida é `localStorage`, ativada pelo checkbox `Manter conectado`; `sessionStorage` deve permanecer vazio.
- Executar a spec isolada, toda a suíte Chromium e a spec com `--repeat-each=2`.
- Gates adicionais: `npm --prefix frontend run typecheck` e `npm --prefix frontend run lint`, todos com exit code `0`.
- Commit de implementação: `test(TASK-117): criar jornada E2E de autenticação`.

---

## File Map

- Create: `frontend/e2e/support/authApi.ts` — dados determinísticos da jornada, contratos dos mocks, backend em memória, roteamento HTTP e contadores exatos.
- Create: `frontend/e2e/fixtures.ts` — extensão da fixture Playwright, limpeza automática de contexto/storage, instalação e teardown do backend simulado.
- Create: `frontend/e2e/auth.spec.ts` — jornada cadastro → confirmação → login → rota protegida → refresh → logout → bloqueio.
- Modify: `frontend/e2e/smoke.spec.ts` — consumir a fixture compartilhada para que a suíte completa também rejeite rede não declarada e preserve isolamento.

## Interfaces compartilhadas

`frontend/e2e/support/authApi.ts` produzirá:

```ts
export type RequestName = 'register' | 'login' | 'categories' | 'profile' | 'logout'
export type ExpectedRequestCounts = Partial<Record<RequestName, number>>

export type RegistrationData = {
  customerId: number
  userId: number
  cpf: string
  name: string
  birthDate: string
  email: string
  password: string
  street: string
  number: string
  postalCode: string
  district: string
  city: string
  state: string
  areaCode: string
  phone: string
}

export type AuthApi = {
  data: RegistrationData
  expectRequestCounts(expected: ExpectedRequestCounts): void
  assertRequestCounts(): void
  reset(): void
}

export function buildRegistrationData(testInfo: TestInfo): RegistrationData
export async function installAuthApi(
  context: BrowserContext,
  testInfo: TestInfo,
): Promise<AuthApi>
```

`frontend/e2e/fixtures.ts` produzirá:

```ts
export const test: TestType<
  PlaywrightTestArgs & PlaywrightTestOptions & { authApi: AuthApi },
  PlaywrightWorkerArgs & PlaywrightWorkerOptions
>
export { expect }
```

As specs importarão exclusivamente:

```ts
import { expect, test } from './fixtures'
```

---

### Task 1: Criar o backend determinístico por teste

**Files:**
- Create: `frontend/e2e/support/authApi.ts`
- Test: `frontend/e2e/auth.spec.ts`

**Interfaces:**
- Consumes: `BrowserContext`, `Route` e `TestInfo` de `@playwright/test`.
- Produces: `AuthApi`, `RegistrationData`, `ExpectedRequestCounts`, `buildRegistrationData` e `installAuthApi` com as assinaturas do bloco compartilhado.

- [ ] **Step 1: Criar uma spec mínima que exija a fixture ainda inexistente**

Crie `frontend/e2e/auth.spec.ts`:

```ts
import { expect, test } from './fixtures'

test('cadastra, autentica, preserva a sessão local e encerra o acesso protegido', async ({
  authApi,
  page,
}) => {
  authApi.expectRequestCounts({
    register: 1,
    login: 1,
    categories: 2,
    profile: 2,
    logout: 1,
  })

  await page.goto('/cadastro')
  await expect(page.getByRole('heading', { level: 1, name: 'Cadastro de cliente' })).toBeVisible()
})
```

- [ ] **Step 2: Executar a spec e confirmar o RED por módulo ausente**

Run:

```bash
npm --prefix frontend run test:e2e -- auth.spec.ts --project=chromium
```

Expected: FAIL durante a compilação com erro equivalente a `Cannot find module './fixtures'`.

- [ ] **Step 3: Implementar dados nomeados, estado isolado, roteamento estrito e contadores**

Crie `frontend/e2e/support/authApi.ts`:

```ts
import type { BrowserContext, Route, TestInfo } from '@playwright/test'

const API_ORIGIN = 'http://localhost:5228'
const API_PATTERN = `${API_ORIGIN}/api/v1/**`
const TOKEN = 'task-117.header.payload'

export type RequestName = 'register' | 'login' | 'categories' | 'profile' | 'logout'
export type ExpectedRequestCounts = Partial<Record<RequestName, number>>

export type RegistrationData = {
  customerId: number
  userId: number
  cpf: string
  name: string
  birthDate: string
  email: string
  password: string
  street: string
  number: string
  postalCode: string
  district: string
  city: string
  state: string
  areaCode: string
  phone: string
}

export type AuthApi = {
  data: RegistrationData
  expectRequestCounts(expected: ExpectedRequestCounts): void
  assertRequestCounts(): void
  reset(): void
}

type RegistrationRequest = {
  senha: string
  cpf: string
  nome: string
  dataNascimento: string
  email: string
  endereco: {
    logradouro: string
    numero: string
    complemento: string | null
    cep: string
    bairro: string
    cidade: string
    uf: string
  }
  celular: { ddd: string; numero: string; whatsApp: boolean }
}

type LoginRequest = { email: string; senha: string }

function numericSeed(testInfo: TestInfo) {
  const source = `${testInfo.titlePath.join('|')}|${testInfo.workerIndex}|${testInfo.repeatEachIndex}`
  return [...source].reduce((hash, character) => (hash * 31 + character.charCodeAt(0)) % 10_000, 0)
}

export function buildRegistrationData(testInfo: TestInfo): RegistrationData {
  const seed = numericSeed(testInfo)
  const suffix = seed.toString().padStart(4, '0')

  return {
    customerId: 20_000 + seed,
    userId: 10_000 + seed,
    cpf: `9000000${suffix}`,
    name: `Cliente TASK-117 ${suffix}`,
    birthDate: '1990-05-20',
    email: `task-117-${suffix}@example.test`,
    password: `Senha@${suffix}`,
    street: `Rua TASK-117 ${suffix}`,
    number: '117',
    postalCode: '12345678',
    district: 'Centro',
    city: 'São Paulo',
    state: 'SP',
    areaCode: '11',
    phone: `91234${suffix}`,
  }
}

function json(route: Route, body: unknown, status = 200) {
  return route.fulfill({
    status,
    contentType: 'application/json',
    body: JSON.stringify(body),
  })
}

function readJson<T>(route: Route): T {
  return route.request().postDataJSON() as T
}

function requireMethod(route: Route, expected: string) {
  const actual = route.request().method()
  if (actual !== expected) {
    throw new Error(`Expected ${expected} ${route.request().url()}, received ${actual}`)
  }
}

function requireAuthorization(route: Route) {
  const authorization = route.request().headers().authorization
  if (authorization !== `Bearer ${TOKEN}`) {
    throw new Error(`Expected Bearer token for ${route.request().method()} ${route.request().url()}`)
  }
}

export async function installAuthApi(
  context: BrowserContext,
  testInfo: TestInfo,
): Promise<AuthApi> {
  const data = buildRegistrationData(testInfo)
  const counts: Record<RequestName, number> = {
    register: 0,
    login: 0,
    categories: 0,
    profile: 0,
    logout: 0,
  }
  let expected: ExpectedRequestCounts = {}
  let registeredCustomer: RegistrationRequest | null = null

  const increment = (name: RequestName) => {
    counts[name] += 1
  }

  await context.route(API_PATTERN, async (route) => {
    const request = route.request()
    const url = new URL(request.url())

    if (url.pathname === '/api/v1/cliente' && request.method() === 'POST') {
      increment('register')
      const body = readJson<RegistrationRequest>(route)
      const expectedBody: RegistrationRequest = {
        senha: data.password,
        cpf: data.cpf,
        nome: data.name,
        dataNascimento: data.birthDate,
        email: data.email,
        endereco: {
          logradouro: data.street,
          numero: data.number,
          complemento: null,
          cep: data.postalCode,
          bairro: data.district,
          cidade: data.city,
          uf: data.state,
        },
        celular: {
          ddd: data.areaCode,
          numero: data.phone,
          whatsApp: true,
        },
      }
      if (JSON.stringify(body) !== JSON.stringify(expectedBody)) {
        throw new Error(`Unexpected registration body: ${JSON.stringify(body)}`)
      }
      registeredCustomer = body
      await json(route, { status: true, data: { clienteId: data.customerId } }, 201)
      return
    }

    if (url.pathname === '/api/v1/auth/login') {
      requireMethod(route, 'POST')
      increment('login')
      const body = readJson<LoginRequest>(route)
      if (
        registeredCustomer === null
        || body.email !== registeredCustomer.email
        || body.senha !== registeredCustomer.senha
      ) {
        throw new Error(`Unexpected login body: ${JSON.stringify(body)}`)
      }
      await json(route, {
        status: true,
        data: {
          token: TOKEN,
          tipo: 'Bearer',
          expiraEm: '2099-12-31T23:59:59-03:00',
          usuarioId: data.userId,
          clienteId: data.customerId,
          email: data.email,
        },
      })
      return
    }

    if (url.pathname === '/api/v1/categoria') {
      requireMethod(route, 'GET')
      increment('categories')
      await json(route, { status: true, data: [] })
      return
    }

    if (url.pathname === `/api/v1/cliente/${data.customerId}`) {
      requireMethod(route, 'GET')
      requireAuthorization(route)
      increment('profile')
      if (registeredCustomer === null) {
        throw new Error('Profile requested before registration')
      }
      await json(route, {
        status: true,
        data: {
          clienteId: data.customerId,
          cpf: registeredCustomer.cpf,
          nome: registeredCustomer.nome,
          dataNascimento: registeredCustomer.dataNascimento,
          email: registeredCustomer.email,
          endereco: registeredCustomer.endereco,
          celular: registeredCustomer.celular,
        },
      })
      return
    }

    if (url.pathname === '/api/v1/auth/logout') {
      requireMethod(route, 'POST')
      requireAuthorization(route)
      increment('logout')
      await route.fulfill({ status: 204, body: '' })
      return
    }

    await route.abort('blockedbyclient')
    throw new Error(`Unexpected API request: ${request.method()} ${request.url()}`)
  })

  return {
    data,
    expectRequestCounts(nextExpected) {
      expected = { ...nextExpected }
    },
    assertRequestCounts() {
      const mismatches = (Object.keys(counts) as RequestName[]).flatMap((name) => {
        const wanted = expected[name] ?? 0
        return counts[name] === wanted ? [] : [`${name}: expected ${wanted}, received ${counts[name]}`]
      })
      if (mismatches.length > 0) {
        throw new Error(`Request count mismatch:\n${mismatches.join('\n')}`)
      }
    },
    reset() {
      registeredCustomer = null
      expected = {}
      ;(Object.keys(counts) as RequestName[]).forEach((name) => {
        counts[name] = 0
      })
    },
  }
}
```

- [ ] **Step 4: Validar estaticamente o helper**

Run:

```bash
npm --prefix frontend run lint -- e2e/support/authApi.ts
```

Expected: exit code `0`, sem erros ESLint.

- [ ] **Step 5: Commitar o backend simulado isoladamente**

```bash
git add frontend/e2e/support/authApi.ts frontend/e2e/auth.spec.ts
git commit -m "test(TASK-117): criar backend E2E deterministico"
```

Expected: commit criado e `git status --short` sem mudanças nesses dois arquivos.

---

### Task 2: Criar a fixture automática de isolamento e rede

**Files:**
- Create: `frontend/e2e/fixtures.ts`
- Modify: `frontend/e2e/smoke.spec.ts`
- Test: `frontend/e2e/auth.spec.ts`

**Interfaces:**
- Consumes: `installAuthApi(context, testInfo): Promise<AuthApi>`.
- Produces: `test` com fixture automática `authApi` e reexport de `expect`.

- [ ] **Step 1: Criar a fixture com limpeza antes e depois de cada teste**

Crie `frontend/e2e/fixtures.ts`:

```ts
import {
  expect,
  test as base,
  type PlaywrightTestArgs,
  type PlaywrightTestOptions,
  type PlaywrightWorkerArgs,
  type PlaywrightWorkerOptions,
  type TestType,
} from '@playwright/test'

import { installAuthApi, type AuthApi } from './support/authApi'

type AuthFixtures = { authApi: AuthApi }

const test = base.extend<AuthFixtures>({
  authApi: [async ({ context }, use, testInfo) => {
    await context.clearCookies()
    await context.addInitScript(() => {
      window.localStorage.clear()
      window.sessionStorage.clear()
    })

    const authApi = await installAuthApi(context, testInfo)
    authApi.reset()

    try {
      await use(authApi)
    } finally {
      try {
        authApi.assertRequestCounts()
      } finally {
        await Promise.all(context.pages().map(async (page) => {
          await page.evaluate(() => {
            window.localStorage.clear()
            window.sessionStorage.clear()
          }).catch(() => undefined)
        }))
        await context.clearCookies()
        await context.unrouteAll({ behavior: 'wait' })
        authApi.reset()
      }
    }
  }, { auto: true }],
})

const typedTest: TestType<
  PlaywrightTestArgs & PlaywrightTestOptions & AuthFixtures,
  PlaywrightWorkerArgs & PlaywrightWorkerOptions
> = test

export { expect, typedTest as test }
```

- [ ] **Step 2: Migrar o smoke para a fixture compartilhada e declarar contagens zero**

Substitua `frontend/e2e/smoke.spec.ts` por:

```ts
import { expect, test } from './fixtures'

test('loads the SPA', async ({ authApi, page }) => {
  authApi.expectRequestCounts({})

  await page.goto('/entrar')

  await expect(page).toHaveTitle('shop-api')
  await expect(
    page.getByRole('heading', { level: 1, name: 'Entrar na sua conta' }),
  ).toBeVisible()
  await expect(page.getByRole('link', { name: 'Criar agora' })).toHaveAttribute(
    'href',
    '/cadastro',
  )
})
```

Essa mudança mantém o smoke sem requests porque `/entrar` usa `PublicLayout`; evita que a home dispare catálogo/categorias e garante que qualquer rede futura não declarada falhe.

- [ ] **Step 3: Executar o smoke para verificar a fixture**

Run:

```bash
npm --prefix frontend run test:e2e -- smoke.spec.ts --project=chromium
```

Expected: `1 passed`; nenhum request para a API.

- [ ] **Step 4: Executar a spec parcial e confirmar GREEN da infraestrutura**

Run:

```bash
npm --prefix frontend run test:e2e -- auth.spec.ts --project=chromium
```

Expected: FAIL somente no teardown com contagens `register`, `login`, `categories`, `profile` e `logout` abaixo do esperado; não deve haver erro de import, fixture ou request inesperado.

- [ ] **Step 5: Commitar a fixture compartilhada**

```bash
git add frontend/e2e/fixtures.ts frontend/e2e/smoke.spec.ts
git commit -m "test(TASK-117): isolar contexto e rede do Playwright"
```

Expected: commit criado; `frontend/e2e/auth.spec.ts` permanece sem alteração desde o commit anterior.

---

### Task 3: Completar a jornada cadastro → logout

**Files:**
- Modify: `frontend/e2e/auth.spec.ts`
- Test: `frontend/e2e/auth.spec.ts`

**Interfaces:**
- Consumes: fixture `authApi`, `RegistrationData` e os handlers de cadastro, login, categorias, perfil e logout.
- Produces: uma jornada única, independente e repetível com contagens `{ register: 1, login: 1, categories: 2, profile: 2, logout: 1 }`.

- [ ] **Step 1: Substituir a spec parcial pela jornada completa**

Substitua `frontend/e2e/auth.spec.ts` por:

```ts
import { expect, test } from './fixtures'

test('cadastra, autentica, preserva a sessão local e encerra o acesso protegido', async ({
  authApi,
  page,
}) => {
  const { data } = authApi
  authApi.expectRequestCounts({
    register: 1,
    login: 1,
    categories: 2,
    profile: 2,
    logout: 1,
  })

  await page.goto('/cadastro')
  await expect(page.getByRole('heading', { level: 1, name: 'Cadastro de cliente' })).toBeVisible()

  await page.getByLabel('Nome completo').fill(data.name)
  await page.getByLabel('CPF').fill(data.cpf)
  await page.getByLabel('Data de nascimento').fill(data.birthDate)
  await page.getByLabel('E-mail').fill(data.email)
  await page.getByLabel('Senha').fill(data.password)
  await page.getByLabel('CEP').fill(data.postalCode)
  await page.getByLabel('Logradouro').fill(data.street)
  await page.getByLabel('Número').fill(data.number)
  await page.getByLabel('Bairro').fill(data.district)
  await page.getByLabel('Cidade').fill(data.city)
  await page.getByLabel('UF').fill(data.state)
  await page.getByLabel('Celular').fill(`${data.areaCode}${data.phone}`)
  await page.getByRole('checkbox', {
    name: 'Este celular também é WhatsApp',
  }).check()
  await page.getByRole('button', { name: 'Criar conta' }).click()

  await expect(page).toHaveURL('/entrar')
  await expect(page.getByRole('status')).toContainText('Cadastro concluído')
  await expect(page.getByRole('status')).toContainText(
    'Sua conta foi criada. Entre com as credenciais cadastradas.',
  )

  await expect.poll(async () => page.evaluate(() => ({
    local: localStorage.getItem('shop-api:auth'),
    session: sessionStorage.getItem('shop-api:auth'),
  }))).toEqual({ local: null, session: null })

  await page.goto('/minha-conta/dados')

  await expect(page).toHaveURL('/entrar')
  await expect(
    page.getByRole('heading', { level: 1, name: 'Entrar na sua conta' }),
  ).toBeVisible()

  await page.getByLabel('E-mail').fill(data.email)
  await page.getByLabel('Senha').fill(data.password)
  await page.getByRole('checkbox', { name: 'Manter conectado' }).check()
  await page.getByRole('button', { name: 'Entrar', exact: true }).click()

  await expect(page).toHaveURL('/minha-conta/dados')
  await expect(page.getByRole('heading', { level: 1, name: 'Meus dados' })).toBeVisible()
  await expect(page.getByLabel('Nome completo')).toHaveValue(data.name)

  await expect.poll(async () => page.evaluate(() => ({
    local: localStorage.getItem('shop-api:auth'),
    session: sessionStorage.getItem('shop-api:auth'),
  }))).toEqual({
    local: expect.stringContaining(data.email),
    session: null,
  })

  await page.reload()

  await expect(page).toHaveURL('/minha-conta/dados')
  await expect(page.getByRole('heading', { level: 1, name: 'Meus dados' })).toBeVisible()
  await expect(page.getByLabel('E-mail')).toHaveValue(data.email)

  await page.getByRole('button', { name: 'Área do cliente' }).click()
  await page.getByRole('menuitem', { name: 'Sair' }).click()

  await expect(page).toHaveURL('/entrar')
  await expect.poll(async () => page.evaluate(() => ({
    local: localStorage.getItem('shop-api:auth'),
    session: sessionStorage.getItem('shop-api:auth'),
  }))).toEqual({ local: null, session: null })

  await page.goto('/minha-conta/dados')

  await expect(page).toHaveURL('/entrar')
  await expect(
    page.getByRole('heading', { level: 1, name: 'Entrar na sua conta' }),
  ).toBeVisible()
  await expect(page.queryByRole('heading', { level: 1, name: 'Meus dados' })).not.toBeVisible()
})
```

- [ ] **Step 2: Corrigir a assertion negativa para a API Playwright válida**

No último bloco da spec, substitua:

```ts
await expect(page.queryByRole('heading', { level: 1, name: 'Meus dados' })).not.toBeVisible()
```

por:

```ts
await expect(
  page.getByRole('heading', { level: 1, name: 'Meus dados' }),
).toHaveCount(0)
```

Esta microetapa é explícita porque Playwright não possui `page.queryByRole`; a forma correta preserva o seletor semântico e afirma ausência.

- [ ] **Step 3: Executar a spec isolada**

Run:

```bash
npm --prefix frontend run test:e2e -- auth.spec.ts --project=chromium
```

Expected: `1 passed`; contagens exatas `register=1`, `login=1`, `categories=2`, `profile=2`, `logout=1`; nenhuma chamada adicional.

- [ ] **Step 4: Executar duas repetições para provar isolamento**

Run:

```bash
npm --prefix frontend run test:e2e -- auth.spec.ts --project=chromium --repeat-each=2
```

Expected: `2 passed`; cada repetição usa e-mail, CPF, IDs, estado e contadores próprios.

- [ ] **Step 5: Commitar a jornada completa**

```bash
git add frontend/e2e/auth.spec.ts
git commit -m "test(TASK-117): criar jornada E2E de autenticacao"
```

Expected: commit criado e nenhum arquivo de produto alterado.

---

### Task 4: Executar regressão, revisar e entregar evidências

**Files:**
- Verify: `frontend/e2e/support/authApi.ts`
- Verify: `frontend/e2e/fixtures.ts`
- Verify: `frontend/e2e/auth.spec.ts`
- Verify: `frontend/e2e/smoke.spec.ts`
- Modify only after approvals: `docs/frontend-tasks-v2.md`

**Interfaces:**
- Consumes: implementação completa da TASK-117 e `BASE_COMMIT`.
- Produces: evidências reproduzíveis, diff revisado, aprovação e atualização do backlog feita somente pelo orquestrador.

- [ ] **Step 1: Executar toda a suíte Chromium**

Run:

```bash
npm --prefix frontend run test:e2e -- --project=chromium
```

Expected: todas as specs em `frontend/e2e` passam; nenhuma depende de backend externo.

- [ ] **Step 2: Executar o gate de repetição exigido pelo backlog**

Run:

```bash
npm --prefix frontend run test:e2e -- --project=chromium --repeat-each=2
```

Expected: toda a suíte passa duas vezes, inclusive com `fullyParallel: true`.

- [ ] **Step 3: Executar typecheck e lint**

Run:

```bash
npm --prefix frontend run typecheck
npm --prefix frontend run lint
```

Expected: ambos com exit code `0`; warnings preexistentes devem ser registrados, mas nenhum erro novo é aceito.

- [ ] **Step 4: Confirmar escopo e gerar o diff para revisão**

Run:

```bash
git status --short
git diff --stat "$BASE_COMMIT"..HEAD
git diff "$BASE_COMMIT"..HEAD -- frontend/e2e
```

Expected: somente `frontend/e2e/support/authApi.ts`, `frontend/e2e/fixtures.ts`, `frontend/e2e/auth.spec.ts` e `frontend/e2e/smoke.spec.ts` pertencem à implementação; artefatos como `frontend/playwright-report` e `frontend/test-results` não entram em commit.

- [ ] **Step 5: Delegar revisão e corrigir findings bloqueantes**

O revisor deve verificar:

```text
1. O route mock cobre somente http://localhost:5228/api/v1/** e aborta qualquer endpoint não declarado.
2. Cadastro, login, perfil e logout validam método; perfil/logout validam Bearer token.
3. O corpo completo do cadastro e as credenciais de login são comparados.
4. Estado, storages, cookies e contadores são isolados por teste.
5. O teardown roda em finally e remove rotas/cookies mesmo após falha.
6. A jornada usa somente seletores semânticos.
7. Persistência local sobrevive ao refresh e logout limpa os dois storages.
8. O acesso direto à rota protegida após logout termina em /entrar.
9. Contagens exatas são afirmadas para todos os nomes, inclusive zero implícito.
10. Execução isolada, suíte Chromium, repeat-each=2, typecheck e lint passam.
```

Expected: nenhum finding `CRITICAL` ou `IMPORTANT`. Se houver, devolver ao mesmo agente implementador, repetir todos os gates e reenviar ao revisor.

- [ ] **Step 6: Consolidar o commit exigido pela task se necessário**

Se os commits intermediários ainda não estiverem no formato final exigido pelo backlog, o orquestrador deve preservar a rastreabilidade e criar um commit corretivo final:

```bash
git add frontend/e2e
git commit -m "test(TASK-117): criar jornada E2E de autenticacao"
```

Expected: não criar commit vazio; os commits da task ficam claramente identificados por `TASK-117`.

- [ ] **Step 7: Atualizar backlog somente após ambas as aprovações**

Após aprovação do implementador e do revisor, alterar em `docs/frontend-tasks-v2.md` apenas:

```markdown
[x] TASK-117: Criar E2E de cadastro, login, rota protegida e logout.
  - Status: DONE
```

Adicionar no registro da task as evidências reais de comandos e os hashes reais dos commits produzidos. Não antecipar valores no plano.

- [ ] **Step 8: Commitar a atualização do backlog**

```bash
git add docs/frontend-tasks-v2.md
git commit -m "docs(TASK-117): registrar conclusao da jornada E2E"
```

Expected: backlog marca somente TASK-117 como `DONE`; TASK-118 a TASK-123 continuam com o status permitido por suas dependências.

## Self-Review

- Spec coverage: fixtures determinísticas, dados nomeados, backend/storage/cookies isolados, cleanup em `finally`, requests não declarados, cadastro, confirmação, login, rota protegida, persistência após refresh, logout, remoção de acesso, contagens exatas, execução isolada, Chromium e repetição estão mapeados.
- Placeholder scan: o plano não contém `TBD`, `TODO`, “similar ao anterior” ou passos de código sem conteúdo literal.
- Type consistency: `AuthApi`, `RegistrationData`, `ExpectedRequestCounts`, `buildRegistrationData` e `installAuthApi` mantêm os mesmos nomes e assinaturas em todas as tasks.
- Contract consistency: endpoints reais são `/api/v1/cliente`, `/api/v1/auth/login`, `/api/v1/categoria`, `/api/v1/cliente/:clienteId` e `/api/v1/auth/logout`; envelopes e nomes de campos seguem os adapters existentes.
- UI consistency: labels e cópias reais são `Cadastro de cliente`, `Cadastro concluído`, `E-mail`, `Senha`, `Manter conectado`, `Área do cliente`, `Meus dados` e `Sair`.
